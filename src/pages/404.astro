---
// Static 404. Astro emits a single /404.html, so it renders in the default
// language (English). Cloudflare Pages serves it for unmatched routes.
import NotFoundPage from '../components/pages/NotFoundPage.astro';
---

<NotFoundPage lang="en" />
